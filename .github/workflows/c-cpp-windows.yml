name: Windows C/C++ CI

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Download SDL2
      run: Get-Location
    - name: Prepare Tools
      run: |
           c++ --version
