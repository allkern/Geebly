name: Windows C/C++ CI

on:
  push:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Download and setup SDL2
      run: |
           tar --version
    - name: Prepare tools
      run: |
           c++ --version
